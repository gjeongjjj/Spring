<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapperInterface.JoMapper">


<!-- ** joCaptain -->
	<select id="joCaptain" resultType="com.ncs.spring02.domain.JoDTO">
<!-- 		select j.jno, j.jname, j.captain, m.name, j.project, j.slogan 
		from member m 
		join jo j 
		on j.captain = m.id -->
		
		select j.jno, j.jname, captain, m.name cname, project, slogan 
		from jo j 
		join member m 
		ON m.id = j.captain
		
	</select>
	
<!-- ** joList -->
	<select id="joList" resultType="com.ncs.spring02.domain.JoDTO">
		select jno, jname from jo
	</select>

<!-- ** joDetail -->
	<select id="joDetail" resultType="com.ncs.spring02.domain.JoDTO" >
		select * from jo where jno = #{jno}
	</select>

<!-- ** joInsert  -->
	<insert id="joInsert">
		insert into jo values (#{jno}, #{jname}
			, #{captain}, #{project}, #{slogan})
	</insert>

<!-- ** joUpdate -->
	<update id="joUpdate">
		update jo set jname = #{jname}, captain = #{captain}
			, project = #{project}, slogan = #{slogan} where jno = #{jno}
	</update>

<!-- ** joDelete -->
	<delete id="joDelete">
		delete from jo where jno = #{jno}
	</delete>




</mapper>